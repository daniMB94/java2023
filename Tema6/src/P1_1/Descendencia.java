package P1_1;

public class Descendencia {

	//Si podrá tener descendencia a pesar de no tener constructor porque a pesar de ser una
	//clase abstracta y no poder crearse objetos de ella si se pueden crear objetos de 
	//las clases hijas que hereden de vehículo
	
	//El método getVelocidadActual() si se puede sobreescribir pero el método setPeso no se podría
	//sobreescribir porque lleva la palabra "final"
	
}
